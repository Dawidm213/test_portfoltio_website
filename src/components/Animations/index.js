import { motion, AnimatePresence } from "framer-motion";

import { Container, Wrapper, } from './Animations.styles';

const Animations = () => {

  return(
    <>
      <Wrapper>
        <Container>
            <button id="animation" >cos sie wymysli</button>
        </Container>
    </Wrapper>
    </>
  )
};

export default Animations;
